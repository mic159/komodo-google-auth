# To install, add the below to your configuration YAML file

plugins:
  - import_path: komodo_google_auth:SocialAuthPlugin
    options:
      client_id: CHANGEME
      client_secret: CHANGEME
      restrict_emails:
        - user1@example.com
      restrict_domains:
        - mycompany.com
        - example.com
